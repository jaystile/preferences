#################################################
# Neovim NVIM
#################################################
ARCH=$(uname -m)
export NVIM_HOME=/opt/nvim-linux-${ARCH}
export PATH=${PATH}:${NVIM_HOME}/bin
